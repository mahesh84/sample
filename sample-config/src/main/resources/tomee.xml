<?xml version="1.0" encoding="UTF-8"?>
<tomee>
	<Resource id="${jdbc.id}" type="DataSource">
		JdbcDriver ${database.driver}
		JdbcUrl ${database.url}
		UserName ${database.username}
		Password ${database.password}
		JtaManaged ${database.jtamanaged}
		InitialSize ${database.initialsize}
		MaxActive ${database.MaxActive}
		TestOnBorrow ${database.TestOnBorrow}
		PoolPreparedStatements ${database.PoolPreparedStatements}
		MaxOpenPreparedStatements ${database.MaxOpenPreparedStatements}
	</Resource>
</tomee>